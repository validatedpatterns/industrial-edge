apiVersion: apps/v1
kind: Deployment
metadata:
  name: mqtt-messaging
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: mqtt-messaging
  template:
    metadata:
      labels:
        app: mqtt-messaging
    spec:
      containers:
      - image: openshiftkatacoda/blog-django-py
        name: blog-from-image-pod-container
        ports:
        - containerPort: 8080
