apiVersion: v1
kind: ConfigMap
metadata:
  name: line-dashboard-configmap-
  namespace: manuela-tst-all
  labels:
    app.kubernetes.io/instance: manuela-tst-all
data:
  //config.json: {{ toJson .Values.lineDashboard.config | toJson | quote }}
  config.json: {{ toJson .Values.lineDashboard.config  }}
